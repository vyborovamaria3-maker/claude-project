"use client";

import type { StepProps } from "./types";
import LaunchStep from "./step5/LaunchStep";

// data-tag: token_launch.new.step5
export default function TLstep_5(props: StepProps) {
  return <LaunchStep tag="token_launch.new.step5" {...props} />;
}
