name: solana-launcher-deploy (deprecated)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  deprecated:
    name: Deployment workflow moved
    runs-on: ubuntu-latest

    steps:
      - name: Show migration notice
        run: |
          echo "This workflow no longer deploys the application."
          echo "Use the POTAPoff production workflow."
